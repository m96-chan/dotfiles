btop — brew install btop
wtf — brew install wtfutil
circumflex — brew install circumflex
ghq + fzf — brew install ghq fzf
starship — brew install starship
kitty — brew install --cask kitty

# Rust がなければ先に入れる
sudo pacman -S rust

# tickrs インストール
cargo install tickrs